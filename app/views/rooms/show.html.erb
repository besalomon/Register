<!DOCTYPE html>
<html>
<head>
  <title>View/Edit Room</title>
</head>
<body>
<h1>View/Edit Room</h1>
<%= form_tag"/rooms/#{params[:id]}", :class => "room", method: :patch do %>
  <div class="form-group">
    <label for="room_name">Room Name</label>
    <input type="text" class="form-control" id="Name" aria-describedby="Room Name" value = "<%= @room.name %>" name ="room_name">
    <small id="FirstNameHelp" class="form-text text-muted" >Eg: Musanze Lounge</small>
  </div>

    <div class="form-group">
    <label for="capacity">Capacity</label>
    <input type="number" class="form-control" id="numberofstudents" aria-describedby="Number of Students" name = "capacity" value = "<%= @room.capacity %>">
  </div>

  <div class="form-group">
    <label for="last_name">Created_by</label>
    <input type="text" class="form-control" id="LastName" aria-describedby="Created by" value = "<%= @room.user.first_name + " " + @room.user.last_name %>" disabled>
  </div>

<button type="submit" class="btn btn-primary">Update room Info</button>
  <% end %>
</body>
</html>